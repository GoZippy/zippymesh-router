/**
 * User-facing display labels for internal terminology.
 * ONLY use for UI display strings — do NOT change API routes,
 * database columns, React prop names, or any non-display code.
 */
export const DISPLAY_NAMES = {
  pools: 'Provider Groups',
  combos: 'Fallback Chains',
  playbooks: 'Routing Rules',
  intent: 'Request Type',
  sidecar: 'Local Service',
  endpoint: 'API Server',
  fallback: 'Backup',
};
