# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
# Tauri build artifacts (generated — do not commit)
/src-tauri/resources/
/src-tauri/binaries/*.exe
/src-tauri/binaries/*.bin
/src-tauri/target/
# Windows build-retry dist dirs AND isolated verification builds
# (ZIPPY_NEXT_DIST_DIR=.next-a12 / .next-f2 / etc.)
/.next-*/
/out/

# production
/build
cloud/*

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.bin/*
data/
logs/*
source/*
.cursor/*
docs/*
# docs/_internal/ holds plans, session reports, internal runbooks.
# It MUST be tracked in the private `origin` remote (so devs share context),
# but build-community.cjs excludes it so it never reaches the public repo.
!docs/_internal/
!docs/_internal/**
!docs/ARCHITECTURE.md
!docs/REPO_ROLES.md
!docs/OPEN_CORE_MANIFEST.md
!docs/RELEASE.md
!docs/RUNNING.md
!docs/PLAYBOOKS.md
!docs/PUBLIC_RELEASE_CHECKLIST.md
!docs/MESH_VERIFICATION.md
!docs/example-playbooks/
!docs/example-playbooks/*.json
!docs/DISTRIBUTION_PLAN.md
!docs/STANDALONE_README.md
!docs/SETUP.md
!docs/node-setup.md
!docs/build-from-source.md
!docs/WALLET_BACKUP_AND_INSTALLER_SAFETY.md
!docs/INSTALLER_AND_SECURE_STORAGE.md
!docs/oauth-setup.md
!docs/zippymesh-licensing-bundle/
!docs/zippymesh-licensing-bundle/*.md
!docs/project_status.md
# Public operator docs added 2026-08-30 (Kiro Crew / Track A+C)
!docs/DOCTOR.md
!docs/MCP_STDIO.md
!docs/ZVAULT_RUN.md
# Linked from docs/SETUP.md but were never tracked (install audit 2026-08-30)
!docs/INSTALLER_MATRIX.md
!docs/API.md
test/*
bin/*
# zvault launcher (Track C) — the only source file that lives in bin/
!bin/zvault.mjs
open-sse/test/*
RM.vn.md
RM.md
cursor/*
PUBLIC.md
scripts/*
!scripts/validate-open-core.cjs
# shared leak-pattern definitions, required by validate-open-core.cjs and secrets-check.cjs
!scripts/leak-patterns.cjs
!scripts/build.cjs
!scripts/prepare-standalone.cjs
!scripts/setup-env.mjs
!scripts/package-release.cjs
!scripts/check-env.js
!scripts/upgrade.sh
!scripts/upgrade.ps1
!scripts/build-community.sh
!scripts/build-community.cjs
!scripts/test-routing.sh
!scripts/zippy-cli.mjs
!scripts/build-tauri-frontend.cjs
!scripts/zippy-launcher.cjs
!scripts/build-sidecar.cjs
# Track A tooling added 2026-08-30: doctor, MCP stdio runner, production-build e2e runner
!scripts/doctor.mjs
!scripts/doctor/
!scripts/doctor/**
!scripts/mcp-stdio.mjs
# Shared release/build helper modules (2026-08-30 adversarial round: zip leak
# scan + Tauri bundle filter). Without these the release scripts MODULE_NOT_FOUND
# on a clean checkout.
!scripts/lib/
!scripts/lib/**
!scripts/e2e/
!scripts/e2e/**
Thanks.md
PUBLIC.en.md
PR/*
package-lock.json


#Ignore vscode AI rules
.github/instructions/codacy.instructions.md
README1.md

# Backup files
*.bak
# Kilo Code agent config — local-only, never ship
.kilocodemodes
# pnpm lock — project standardises on npm; gitignore stray pnpm artifacts
pnpm-lock.yaml
# Claude AI directories
.claude/

# Community dist
/community-dist/
# Explicitly exclude agent worktree scratchpads and release zips under community-dist
community-dist/.claude/
community-dist/.claude/worktrees/
community-dist/dist/*.zip

# Additional build artifacts
.voidspec/
experiments/zmlr-lite/node_modules/
sidecar/target/

# Large build artifacts
dist/*.zip

# Local build logs (tee'd output from npm run build:* commands)
community-build.log
tauri-build.log

# User-specific runtime data (NEVER commit — contains credentials, tokens, playbooks, usage data)
data/
data/**
oauth-secrets.json
db.json
*.sqlite
*.sqlite-shm
*.sqlite-wal

# User playbook overrides (users create their own — templates live in src/shared/constants/defaults.js)
data/playbooks.json
data/playbooks-user.json

# Debug and session logs (may contain prompt/response data)
debug_log.txt
debug_session*.log
startup*.log
zippymesh.log

# Local credential stores (moved to ~/.openclaw per auth refactor)
scripts/creds.mjs
scripts/creds.js

# AutoClaw runtime state
.autoclaw/
