> **STATUS: ACTIVE — canonical codebase.**

This is the canonical ZippyMesh LLM Router implementation — the production-grade inference mesh networking layer with RBAC admin dashboard, privacy controls, and role-bearing authentication. Active development ongoing with sprint-based feature delivery (admin UI, user account management, security hardening).

**Branch**: `main` (development happens here)
**Latest**: v1.2.1+ (recent commits include privacy posture, offline kill-switch, experimental feature toggles, admin RBAC)

No other ZippyMesh Router repos should be developed in parallel. Successor to earlier prototypes in zippymesh-router (v1.1.0 public mirror).
